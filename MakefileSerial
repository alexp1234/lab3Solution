PRJ_NAME=GlobalSum
GCC=g++

OUTPUT=${PRJ_NAME}Serial
SOURCES=${PRJ_NAME}Serial.cpp
HFILES=
CCFLAGS=-std=c++11 -g3 -O1 -lm

all: ${OUTPUT}
${OUTPUT}:
	${GCC} -o ${OUTPUT} ${CCFLAGS} ${HFILES} ${SOURCES}
    
clean:
	rm -f ${OUTPUT}
   
